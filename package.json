{
  "name": "@waves/waves-signature-adapter",
  "version": "0.0.4",
  "types": "src/index.ts",
  "main": "dist/waves-signature-adapter.js",
  "dependencies": {
    "@waves/ledger": "^0.0.4",
    "@waves/waves-signature-generator": "^0.13.0"
  },
  "scripts": {
    "build": "tsc && node_modules/.bin/browserify dist/index.js -s wavesSignatureAdapter -u @waves/waves-signature-generator -o dist/waves-signature-adapter.js",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "browserify": "^16.2.2",
    "typescript": "^2.9.2"
  }
}
