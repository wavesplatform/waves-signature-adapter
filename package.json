{
  "name": "@waves/waves-signature-adapter",
  "version": "0.1.0",
  "types": "src/index.ts",
  "main": "dist/waves-signature-adapter.js",
  "dependencies": {
    "@waves/data-entities": "^1.6.0",
    "@waves/ledger": "0.0.22",
    "@waves/waves-signature-generator": "0.14.0"
  },
  "scripts": {
    "build": "tsc && node_modules/.bin/browserify dist/index.js -s wavesSignatureAdapter -u @waves/waves-signature-generator -u @waves/ledger -o dist/waves-signature-adapter.js",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "browserify": "^16.2.2",
    "typescript": "^2.9.2"
  }
}
